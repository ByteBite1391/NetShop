"""Common app — shared base models, mixins, response helpers, pagination.

This app has no domain meaning of its own. It holds cross-cutting primitives that
every domain app reuses so we keep DRY and a consistent response shape.
"""
