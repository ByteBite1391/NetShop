# Development extras layered on top of base.txt.
# Includes the full testing + code-quality toolchain.

-r base.txt

# Testing
pytest>=8.2
pytest-django>=4.8
pytest-cov>=5.0
factory-boy>=3.3

# Code quality
black>=24.4
ruff>=0.5
isort>=5.13
mypy>=1.10
django-stubs>=5.0
djangorestframework-stubs>=3.15
pre-commit>=3.7

# Development server (auto-reload) — Django ships with this, but we list it explicitly.
ipdb>=0.13
